name: Desktop integration

on:
  schedule:
    - cron: '17 2 * * 1-5'
  workflow_dispatch:
  push:
    paths:
      - src/**
      - electron/**
      - assets/**
      - START_CURVIOSCLASH.cmd
      - '*.bat'
      - server.ps1
      - scripts/start-editor.ps1
      - server/**
      - editor/**
      - prototypes/vehicle-lab/**
      - tests/**
      - scripts/run-playwright-*.mjs
      - playwright.config.js
      - dev/vite/**
      - vite.config.js
      - package.json
      - package-lock.json
      - electron/package.json
      - electron/package-lock.json
      - .github/workflows/desktop.yml
  pull_request:
    paths:
      - src/**
      - electron/**
      - assets/**
      - START_CURVIOSCLASH.cmd
      - '*.bat'
      - server.ps1
      - scripts/start-editor.ps1
      - server/**
      - editor/**
      - prototypes/vehicle-lab/**
      - tests/**
      - scripts/run-playwright-*.mjs
      - playwright.config.js
      - dev/vite/**
      - vite.config.js
      - package.json
      - package-lock.json
      - electron/package.json
      - electron/package-lock.json
      - .github/workflows/desktop.yml

permissions:
  contents: read

concurrency:
  group: desktop-${{ github.ref }}
  cancel-in-progress: true

jobs:
  smoke:
    runs-on: windows-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version-file: .nvmrc
          cache: npm
          cache-dependency-path: |
            package-lock.json
            electron/package-lock.json
      - run: npm ci
      - run: npm --prefix electron ci
      - name: Install Playwright browser
        run: npx playwright install chromium
      - name: Desktop smoke
        run: npm run test:desktop:smoke

  e2e:
    runs-on: windows-latest
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        cluster:
          - core-shell
          - core-platform
          - core-surface
          - core-runtime
          - core-regressions
          - network
          - desktop-flows
          - gameplay-smoke
          - editor
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version-file: .nvmrc
          cache: npm
          cache-dependency-path: |
            package-lock.json
            electron/package-lock.json
      - run: npm ci
      - run: npm --prefix electron ci
      - name: Install Playwright browser
        run: npx playwright install chromium
      - name: Run ${{ matrix.cluster }} cluster
        run: npm run test:desktop:e2e -- ${{ matrix.cluster }} --timeout=180000

  heavy-e2e:
    if: ${{ github.event_name == 'schedule' || github.event_name == 'workflow_dispatch' }}
    runs-on: windows-latest
    timeout-minutes: 60
    strategy:
      fail-fast: false
      matrix:
        cluster:
          - physics-core
          - physics-hunt
          - physics-policy
          - gpu-stress
          - ghost-selfduel
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version-file: .nvmrc
          cache: npm
          cache-dependency-path: |
            package-lock.json
            electron/package-lock.json
      - run: npm ci
      - run: npm --prefix electron ci
      - name: Install Playwright browser
        run: npx playwright install chromium
      - name: Run ${{ matrix.cluster }} cluster
        run: npm run test:desktop:e2e -- ${{ matrix.cluster }} --timeout=240000

  browser-compat:
    needs: smoke
    runs-on: windows-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version-file: .nvmrc
          cache: npm
          cache-dependency-path: package-lock.json
      - run: npm ci
      - name: Install Playwright browser
        run: npx playwright install chromium
      - name: Browser compatibility smoke
        run: npm run test:browser:compat
